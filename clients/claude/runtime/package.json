{
  "name": "@membase/claude-membase-runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Claude Code plugin runtime for Membase (stdio MCP server, hooks, commands, skills).",
  "type": "module",
  "engines": {
    "node": ">=18.17.0"
  },
  "license": "MIT",
  "scripts": {
    "build": "bun run scripts/build.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit --pretty false"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.22.0",
    "zod": "^4.1.12",
    "@membase/capture-core": "workspace:*"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.6",
    "@types/bun": "^1.3.1",
    "@types/node": "^22.10.2",
    "esbuild": "^0.25.12",
    "typescript": "5.9.2"
  }
}