---
description: Check Membase auth, capture, project, spool, and duplicate MCP status.
---

Call the Membase MCP `get_status` tool.

Use the output to verify whether Membase is connected, show only safe
account-identifying profile fields, report `session_start_context`, and detect
any older remote MCP configuration that may duplicate plugin-local tools. Do not
store, update, or delete anything.
