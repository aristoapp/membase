# Automation (retired)

The scheduled Codex LaunchAgent loop (`run-loop.sh`, `codex-prompt.md`,
`com.aristo.membase-plugin-mcp-loop.plist`) was retired on 2026-07-05.

All implementation work now happens in interactive Cursor or Claude Code
sessions. See `PLAN.md` § Development Workflow.

Historical automation entries remain in `RUN_LOG.md` for reference only.
